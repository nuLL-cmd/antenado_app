<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.automatodev.antenado">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Antenado">
        <activity android:name=".activities.SearchActivity"></activity>
        <activity
            android:name=".activities.FavouritesActivity"
            android:parentActivityName=".activities.FavouritesActivity"
            android:theme="@style/Theme.Antenado.NoActionBar"></activity>
        <activity
            android:name=".activities.DetailsActivity"
            android:label="@string/title_activity_details"
            android:parentActivityName=".activities.DetailsActivity"
            android:theme="@style/Theme.Antenadotwo"></activity>
        <activity
            android:name=".activities.ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Antenado.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>